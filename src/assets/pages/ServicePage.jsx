import React from 'react';
import Header from '../component/Header';

const ServicePage = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default ServicePage;